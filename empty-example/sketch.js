function setup() {
  // put setup code here
 createCanvas(640, 480);
}

function draw() {
  // put drawing code here

 
  if (mouseIsPressed) {
    fill(0);
//    noStroke();
      ellipse(mouseX, mouseY, 20, 20);
  } else {
 
  }
 
}
